///
/// HybridContextMenuViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridContextMenuViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitrocontextmenu::views {

  extern const char HybridContextMenuViewComponentName[] = "ContextMenuView";

  HybridContextMenuViewProps::HybridContextMenuViewProps(const react::PropsParserContext& context,
                                                         const HybridContextMenuViewProps& sourceProps,
                                                         const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    menuConfigJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("menuConfigJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.menuConfigJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.menuConfigJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.menuConfigJson: ") + exc.what());
      }
    }()),
    previewConfigJson([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("previewConfigJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.previewConfigJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.previewConfigJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.previewConfigJson: ") + exc.what());
      }
    }()),
    onPressAction([&]() -> CachedProp<std::function<void(const std::string& /* actionKey */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onPressAction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onPressAction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const std::string& /* actionKey */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onPressAction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.onPressAction: ") + exc.what());
      }
    }()),
    onMenuWillShow([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onMenuWillShow", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onMenuWillShow;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onMenuWillShow);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.onMenuWillShow: ") + exc.what());
      }
    }()),
    onMenuWillHide([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onMenuWillHide", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onMenuWillHide;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onMenuWillHide);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.onMenuWillHide: ") + exc.what());
      }
    }()),
    onPreviewPress([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onPreviewPress", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onPreviewPress;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onPreviewPress);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.onPreviewPress: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridContextMenuViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridContextMenuViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ContextMenuView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridContextMenuViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("menuConfigJson"): return true;
      case hashString("previewConfigJson"): return true;
      case hashString("onPressAction"): return true;
      case hashString("onMenuWillShow"): return true;
      case hashString("onMenuWillHide"): return true;
      case hashString("onPreviewPress"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridContextMenuViewComponentDescriptor::HybridContextMenuViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridContextMenuViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                           const std::shared_ptr<const react::Props>& props,
                                                                                           react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridContextMenuViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridContextMenuViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridContextMenuViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridContextMenuViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridContextMenuViewProps>& props = std::const_pointer_cast<HybridContextMenuViewProps>(constProps);
    HybridContextMenuViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitrocontextmenu::views
