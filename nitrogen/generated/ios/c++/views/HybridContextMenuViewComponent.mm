///
/// HybridContextMenuViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridContextMenuViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridContextMenuViewSpecSwift.hpp"
#import "NitroPlatformComponents-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nitroplatformcomponents;
using namespace margelo::nitro::nitroplatformcomponents::views;

/**
 * Represents the React Native View holder for the Nitro "ContextMenuView" HybridView.
 */
@interface HybridContextMenuViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridContextMenuViewComponent {
  std::shared_ptr<HybridContextMenuViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridContextMenuViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridContextMenuViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridContextMenuViewSpec> hybridView = NitroPlatformComponents::NitroPlatformComponentsAutolinking::createContextMenuView();
    _hybridView = std::dynamic_pointer_cast<HybridContextMenuViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroPlatformComponents::HybridContextMenuViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridContextMenuViewProps const>(props);
  auto& newViewProps = const_cast<HybridContextMenuViewProps&>(newViewPropsConst);
  NitroPlatformComponents::HybridContextMenuViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // menuConfigJson: string
  if (newViewProps.menuConfigJson.isDirty) {
    swiftPart.setMenuConfigJson(newViewProps.menuConfigJson.value);
    newViewProps.menuConfigJson.isDirty = false;
  }
  // previewConfigJson: string
  if (newViewProps.previewConfigJson.isDirty) {
    swiftPart.setPreviewConfigJson(newViewProps.previewConfigJson.value);
    newViewProps.previewConfigJson.isDirty = false;
  }
  // onPressAction: function
  if (newViewProps.onPressAction.isDirty) {
    swiftPart.setOnPressAction(newViewProps.onPressAction.value);
    newViewProps.onPressAction.isDirty = false;
  }
  // onMenuWillShow: function
  if (newViewProps.onMenuWillShow.isDirty) {
    swiftPart.setOnMenuWillShow(newViewProps.onMenuWillShow.value);
    newViewProps.onMenuWillShow.isDirty = false;
  }
  // onMenuWillHide: function
  if (newViewProps.onMenuWillHide.isDirty) {
    swiftPart.setOnMenuWillHide(newViewProps.onMenuWillHide.value);
    newViewProps.onMenuWillHide.isDirty = false;
  }
  // onPreviewPress: function
  if (newViewProps.onPreviewPress.isDirty) {
    swiftPart.setOnPreviewPress(newViewProps.onPreviewPress.value);
    newViewProps.onPreviewPress.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroPlatformComponents::NitroPlatformComponentsAutolinking::isContextMenuViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroPlatformComponents::HybridContextMenuViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
